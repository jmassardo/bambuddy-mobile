'use strict';

const {
  G1,
  G2,
} = require('./github-governance-boundary-contract-finding');
const {
  G3,
  G4,
  G9,
} = require('./github-governance-boundary-formulas');
const {G5, G6} = require('./github-governance-boundary-path-depth');
const {
  G7,
  G8,
} = require('./github-governance-boundary-descriptor-ordering');

module.exports = Object.freeze({
  G1,
  G2,
  G3,
  G4,
  G5,
  G6,
  G7,
  G8,
  G9,
});
