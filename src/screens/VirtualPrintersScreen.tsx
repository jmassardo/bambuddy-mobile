import { EmptyState } from '@/components/common/StateScreens';

// Placeholder for issue #68. Replace this screen rather than extending it.
export default function VirtualPrintersScreen() {
  return (
    <EmptyState
      icon="printer"
      title="Virtual Printers"
      message="Virtual printer management is coming soon."
    />
  );
}
